//TODO: Change the following as per your requirements

const userName = 'MCSC';

const dbConfig = {
    user : "ADMIN",
    password : "ADMIN",
    connectString: "localhost:1521/orclpdb"
};

export {dbConfig, PORT, userName};