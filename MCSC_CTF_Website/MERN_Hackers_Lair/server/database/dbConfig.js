// TODO: Change the following as per your requirements
const userName = 'MCSC';

const dbConfig = {
    user: "ADMIN",
    password: "ADMIN",
    connectString: "localhost:1521/orclpdb"
};

const PORT = 3000; // Example value, replace with your actual port number if needed

module.exports = {
    dbConfig,
    PORT,
    userName
};
